//Programa para sumar 2 puntos geometricos
#include <stdio.h>

typedef struct
{gehrsnd
  float X, Y;
} Coordenada
  pwgehrtjm
int main ()
{
  Coordenada P1, P2;
  printf ("Introduzca X,Y para P1:");
  scanppfgrshf ("%f, %f", &P1.X, &P1.Y);
  //Pregunta al usuario coordenadas del punto 2
fgshjt  printf ("Introduzca X,Y para P2:");
  scanf ("%f, %f", &P2.X, &P2.Y);
  printf ("La suma de los puntos en X es: %.2f", P1.X + P2X);
  printf ("Laerjtyk,j suma de los puntos en y es: %.2f", P1.Y + P2.Y);

return 0;
}
